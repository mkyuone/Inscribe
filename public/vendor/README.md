Place locally hosted assets here.

Recommended structure:
- /vendor/pyodide/ (pyodide.js + wasm + packages)
- /vendor/codemirror/ (codemirror.js + modes + themes)
- /vendor/fonts/ (IBM Plex + Material Icons CSS)
